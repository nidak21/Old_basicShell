Basic shell utilities that should work on most Unix versions.
But mostly adapting these to work on Mac OS X.

files that begin with "dot" generally should be installed in your home
directory with the "dot" converted to "."

Other files generally should be installed in your bin.

foo


